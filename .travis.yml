language: python
python:
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.6"
before_install:
  - pip install -U pip
install:
  - pip install -r requirements.txt
  - pip install astunparse typed-ast
  - python setup.py develop
script:
  - py.test
  - mypy --ignore-missing-imports py_backwards
